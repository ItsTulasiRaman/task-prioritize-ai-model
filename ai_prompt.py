# from bardapi import Bard

# token = 'Ygi_7tvhcIPwq6Nlx2wp2fR2Z2tsjhDVSThGL92FV5pqRgwRvw5O21kA9srV8hkTpUaxlw.'
# bard = Bard(token=token)
# def generate_prompt():
#     inputText = "{Meet George, Buy egg , SCM assignment tonight,}. From the list of todo sort them in terms of importance. Show only the task name and why is it important (don't show the criteria) in the array format as mentioned below ```task 1 - why it is important,task 2 - why it is important```"
#     content = bard.get_answer(inputText)['content']
#     #content = content.split('```')[1]
#     return content

# def input_text_update(textPrompt):
#     inputText = textPrompt + ". From the list of above todo important tasks orderwise. Display all sorted task within an array format [task1,task2]"
#     content = bard.get_answer(inputText)['content']
#     return content

# print(generate_prompt())

